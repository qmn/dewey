#ifndef __UTIL_H__
#define __UTIL_H__

// place utility functions

static inline int max(int a, int b)
{
	return a > b ? a : b;
}

static inline int min(int a, int b)
{
	return a < b ? a : b;
}

#endif /* __UTIL_H__ */
